# Codex 작업 계획

## 목적

이 문서는 `docs/launch-readiness.md`에서 실제 작업을 추출한 Codex용 실행 목록이다. Codex는 사용자에게 별도 판단을 요구하지 않는 `READY` 작업만 수행하고, `WAITING` 작업은 필요한 결정이나 승인이 기록될 때까지 시작하지 않는다.

## 실행 규칙

1. 작업 전 루트 `AGENTS.md`와 관련 문서를 읽는다.
2. Next.js 코드를 수정하기 전 `node_modules/next/dist/docs/`의 관련 가이드를 확인한다.
3. 아래 `READY` 작업을 우선순위와 번호 순서대로 수행한다.
4. 한 작업의 범위를 끝내고 검증한 뒤 상태를 `DONE`으로 변경한다.
5. 요구사항이 불명확하거나 외부 서비스·운영 데이터·비밀값이 필요하면 추측하지 않고 `BLOCKED`로 표시한다.
6. 사용자 원문 없이 인물, 역사, 사상, 연구 내용을 새로 작성하지 않는다.
7. 새로운 의존성, 데이터 모델, 인증 제공자, 배포 서비스는 승인 없이 추가하지 않는다.
8. Production 배포, 운영 DB 변경, 외부 계정 생성, DNS 변경, 실제 메시지 발송은 명시적 승인 없이 수행하지 않는다.
9. 기존 사용자 변경을 보존하고 작업 범위 밖의 파일은 수정하지 않는다.
10. 코드 변경 후 최소한 `npm run lint`와 `npm run build`를 실행한다.

상태 값:

- `READY`: 현재 저장소와 확정된 문서만으로 바로 작업 가능
- `IN_PROGRESS`: 현재 수행 중
- `DONE`: 구현과 검증 완료
- `BLOCKED`: 작업 중 추가 입력 또는 외부 조건이 필요해 중단
- `WAITING`: 사용자 결정 또는 승인이 있어야 시작 가능

## 인증 입력 정책 (OWASP 기준)

- 기준 문서: [OWASP Email Validation and Verification Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Email_Validation_and_Verification_Cheat_Sheet.html), [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html), [OWASP Password Storage Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- 이메일:
  - 특정 국내·해외 포털 도메인 목록으로 가입을 제한하지 않고, 명백히 잘못된 형식만 거부해 유효한 기관·학교·자체 도메인을 넓게 허용한다.
  - 도메인 부분은 소문자로 정규화하되 원본 이메일은 표시·발송용으로 보존하고, 가입·로그인·복구에서 동일한 비교 정책을 사용한다.
  - 이메일을 로그인 아이디로 사용하기 전에 소유권을 확인한다. 확인이 끝나지 않은 계정은 관리자 승인 여부와 관계없이 로그인할 수 없다.
  - 확인 토큰은 암호학적으로 안전한 난수로 생성하고 일회용·만료형으로 운영하며, 원문 토큰과 전체 확인 URL은 DB나 로그에 남기지 않는다.
- 비밀번호:
  - MFA가 없는 현재 서비스에서는 최소 15자, 최대 128자로 허용하고 초과 입력을 자동으로 잘라내지 않는다.
  - 유니코드·공백·특수문자를 포함한 모든 문자를 허용하며 영문자·숫자·특수문자 조합 규칙이나 금지 특수문자 목록을 두지 않는다.
  - 비밀번호 원문은 저장·로그·응답하지 않고 사용자별 salt를 적용한 단방향 해시만 저장한다.
  - 로그인에서도 과도하게 긴 입력을 해시 함수에 전달하지 않도록 동일한 최대 길이를 서버에서 검사한다.
  - 일반적이거나 유출된 비밀번호 차단과 현재 scrypt 작업 계수의 OWASP 충족 여부는 `WAITING-07` 보안 작업에서 검토한다.

## 회원가입 구현 후 확인 사항 (2026-07-21)

| 번호 | 확인 내용 | 정리 및 권장 방향 | 후속 작업 |
| --- | --- | --- | --- |
| 1 | 비밀번호 불일치 시 입력 필드가 초기화됨 | 이름·이메일·동의 상태는 유지하고 비밀번호 필드만 비운다. 비밀번호를 서버 응답 상태에 담아 복원하지 않는다. | `READY-08` |
| 2 | 가입 신청 후 별도 안내 페이지 필요 여부 | 현재 단계에서는 별도 페이지를 추가하지 않는다. 같은 화면에서 승인 대기 상태와 다음 행동을 분명히 안내하고, 향후 신청 조회·온보딩 기능이 생길 때 전용 페이지를 검토한다. | `READY-08` |
| 3 | 랜딩 페이지에서 로그인 페이지로 진입하는 버튼 필요 | 비로그인 사용자에게 데스크톱·모바일 내비게이션 모두 `로그인` 버튼을 제공한다. | `READY-09` |
| 4 | 연구자 로그인 성공 후 `/account`로 이동함 | 연구자 로그인 성공 후 랜딩 페이지(`/`)로 이동한다. 관리자 로그인 후 `/admin` 이동은 유지한다. | `READY-09` |
| 5 | 로그인 후 랜딩 페이지에서 내 정보로 가는 버튼 필요 | 로그인한 연구자에게 `내 정보` 버튼을 제공해 `/account`로 이동하게 한다. 관리자는 `관리자` 버튼을 제공한다. | `READY-09` |
| 6 | 승인제 가입 신청을 관리자 또는 담당자에게 알릴 필요 | 알림 채널 하나만 우선 선택한다. 채널에는 개인정보를 싣지 않고 새 신청이 있다는 사실과 관리자 회원 관리 화면 링크만 전송한다. | `WAITING-10` |
| 7 | 로그인 과정 첫 화면에서 개인정보처리방침 고지 및 동의 필요 | 가입 시 동의를 받는 현재 흐름을 기본안으로 삼고, 로그인할 때마다 재동의를 받지는 않는다. 최초 로그인 또는 방침 변경 때만 동의를 받을지는 별도 결정한다. | `WAITING-11` |
| 8 | 가입 비밀번호 범위와 허용 문자 결정 | OWASP 기준에 따라 MFA가 없는 현재는 15~128자로 하고 모든 문자와 공백을 허용한다. 문자 조합 규칙과 금지 특수문자 목록은 두지 않는다. | `READY-10` |
| 9 | 가입 완료 버튼 문구 변경 | `랜딩 페이지로 돌아가기`를 `홈페이지로 돌아가기`로 변경한다. | `READY-10` |
| 10 | 신청자가 승인 결과를 어떻게 안내받는지 불명확 | 현재는 별도 안내·상태 조회가 없고, 승인되면 로그인할 수 있게 될 뿐이다. 신청자에게 결과를 전달하려면 가입 이메일 알림이 필요하다. | `WAITING-10` |
| 11 | 넓은 이메일 형식 허용 후 소유권 확인 필요 | 포털 도메인 허용 목록은 만들지 않는다. 이메일 확인과 관리자 승인이 모두 끝난 계정만 로그인할 수 있게 한다. | `READY-10`, `WAITING-10` |

## 바로 작업 가능한 일

### READY-01. 문서 정합성 점검

- 상태: `DONE`
- 우선순위: 높음
- 목적: 현재 코드와 운영 문서가 서로 다른 내용을 안내하지 않게 한다.
- 작업:
  - `README.md`, `docs/sitemap.md`, `docs/deployment-checklist.md`, `docs/launch-readiness.md`를 실제 라우트, 스크립트, 인증 방식과 대조한다.
  - 명백히 오래되거나 잘못된 설명만 현재 구현에 맞게 수정한다.
  - Vercel·Supabase의 확정된 구성과 아직 확인하지 않은 설정을 구분하고, 공식 콘텐츠와 회원 정책은 확정된 것처럼 쓰지 않는다.
- 완료 조건:
  - 현재 실행 방법, 공개·관리자 경로, 단일 관리자 인증 방식이 코드와 일치한다.
  - 미결정 사항은 미결정 상태로 남는다.

### READY-02. 랜딩 페이지의 두 탐색 기능 차별화

- 상태: `DONE`
- 우선순위: 높음
- 목적: 연구 주제 허브와 자료 해설형 아카이브가 서로 다른 역할로 인식되게 한다.
- 기준 문서: `docs/proposal.md`, `docs/launch-readiness.md`
- 작업:
  - 기존 홈 컴포넌트와 데이터 흐름을 먼저 확인한다.
  - 연구 주제 허브는 생애·말씀·업적을 보여주는 주요 탐색 영역으로 유지한다.
  - 자료 아카이브는 같은 형태의 주제 카드 반복이 아니라 `처음 읽을 자료` 중심의 조밀한 목록 또는 읽기 경로로 표현한다.
  - 기존 데이터만 사용하고 공식 연구 내용을 임의로 만들지 않는다.
  - 새로운 UI·모션 의존성을 추가하지 않는다.
  - 모바일과 데스크톱에서 정보 위계와 내부 링크를 확인한다.
- 완료 조건:
  - 두 섹션의 제목을 가려도 레이아웃과 정보 구조에서 역할 차이가 보인다.
  - 홈에서 `주제 → 대표 자료 → 다음 자료` 흐름으로 이동할 수 있다.
  - 빈 자료 상태에서도 잘못된 추천이나 존재하지 않는 링크를 노출하지 않는다.
  - `npm run lint`와 `npm run build`가 통과한다.

### READY-03. 주제·자료 연결 무결성 점검

- 상태: `DONE`
- 우선순위: 높음
- 목적: 현재 slug 기반 수동 연결이 조용히 끊어진 상태를 찾고 수정한다.
- 작업:
  - `src/lib/topics.ts`, `src/lib/material-guides.ts`, `prisma/seed.ts`의 주제 slug, 카테고리 slug, 추천 자료 slug를 대조한다.
  - 공개 라우트와 내부 링크의 대상이 실제로 존재하는지 확인한다.
  - 불일치는 기존 구조 안에서 가장 작은 수정으로 해결한다.
  - 공식 콘텐츠를 새로 작성하거나 새 데이터 모델을 만들지 않는다.
- 완료 조건:
  - 생애·말씀·업적 주제의 카테고리 연결이 seed 데이터와 일치한다.
  - 추천 자료가 없을 때 기존 fallback이 정상 작동한다.
  - 확인한 연결 관계와 예외를 관련 운영 문서에 짧게 남긴다.
  - `npm run lint`와 `npm run build`가 통과한다.

### READY-04. 접근성 및 기본 인터페이스 점검

- 상태: `DONE`
- 우선순위: 중간
- 목적: 공식 개시에 필요한 기본 접근성과 조작 피드백을 확보한다.
- 적용 지침: `.agents/skills/apple-design/SKILL.md`
- 작업:
  - 공개 페이지와 관리자 폼의 제목 구조, landmark, label, 키보드 포커스, 링크·버튼 구분을 점검한다.
  - 모바일 터치 영역, 텍스트 대비, 오류·성공 상태 전달을 점검한다.
  - 기존 애니메이션이 있다면 `prefers-reduced-motion` 대응을 확인한다.
  - 명백한 접근성 결함은 기존 CSS와 HTML로 수정한다.
  - Apple 스타일을 이유로 장식 효과나 모션 라이브러리를 추가하지 않는다.
- 완료 조건:
  - 키보드만으로 주요 공개 탐색과 관리자 폼을 사용할 수 있다.
  - 입력 오류와 성공 상태가 색상만으로 전달되지 않는다.
  - 움직임이 필수 정보를 가리거나 reduced-motion 설정을 무시하지 않는다.
  - `npm run lint`와 `npm run build`가 통과한다.

### READY-05. 현재 관리자 운영 안내서 작성

- 상태: `DONE`
- 우선순위: 중간
- 목적: 단일 관리자가 현재 구현된 기능을 안전하게 운영할 수 있게 한다.
- 산출물: `docs/admin-operations.md`
- 작업:
  - 로그인, 게시글 초안·공개·보관, slug 관리, 문의 상태 변경 흐름을 실제 화면과 코드 기준으로 설명한다.
  - 게시글 및 카테고리 slug 변경 시 주제·추천 연결 확인 사항을 포함한다.
  - 환경 변수의 실제 값이나 운영 비밀은 기록하지 않는다.
  - 아직 없는 회원 승인, 파일 업로드, 복원 기능을 있는 것처럼 설명하지 않는다.
- 완료 조건:
  - 새 운영자가 현재 관리자 화면의 지원 범위와 제한을 이해할 수 있다.
  - 데이터 삭제 대신 보관 상태를 사용하는 현재 정책이 명시되어 있다.

### READY-06. 로컬 공식 개시 QA

- 상태: `DONE`
- 우선순위: 중간
- 목적: 배포 전 현재 저장소에서 확인 가능한 공개·관리자 흐름을 검증한다.
- 작업:
  - `npm run lint`와 `npm run build`를 실행한다.
  - 홈, 주제 목록·상세, 자료 목록·상세, 공지 목록·상세, 문의, 로그인, 관리자 경로를 데스크톱과 모바일 크기로 확인한다.
  - 인증이 필요한 변경 작업은 로컬 개발 DB에서만 수행하고, 기존 데이터가 중요하거나 환경이 불명확하면 읽기 검증까지만 한다.
  - 발견한 재현 가능한 결함은 현재 범위 안에서 수정하고 다시 검증한다.
  - 결과와 남은 외부 환경 검증 항목을 `docs/launch-readiness.md`에 날짜와 함께 기록한다.
- 완료 조건:
  - 주요 경로에서 깨진 화면, 잘못된 링크, 처리되지 않은 런타임 오류가 없다.
  - 로컬에서 확인할 수 없는 Production 항목은 완료 처리하지 않는다.

### READY-07. 회원가입과 일반 로그인

- 상태: `DONE`
- 우선순위: 높음
- 목적: 연구원이 이메일과 비밀번호로 가입하고, 관리자 승인 후 로그인할 수 있게 한다.
- 개인정보 기준: `docs/privacy-policy.md`
- 승인된 최소 범위:
  - 공개 회원가입 계정은 `RESEARCHER`, `PENDING` 상태로 생성한다.
  - 관리자가 계정을 승인하거나 비활성화할 수 있다.
  - 승인된 연구원만 로그인할 수 있다.
  - 기존 환경 변수 기반 단일 관리자 로그인은 유지한다.
  - 소셜 로그인, `EDITOR`, 연구 결과 작성 권한, 커뮤니티는 포함하지 않는다.
- 작업:
  - 관련 Next.js 및 Auth.js 가이드를 먼저 확인한다.
  - Prisma에 최소 사용자 모델과 역할·승인 상태를 추가하고 migration을 만든다.
  - 별도 OAuth `Account` 모델은 만들지 않는다.
  - 비밀번호는 평문으로 저장하지 않고 안전한 단방향 해시와 사용자별 salt를 사용한다.
  - `/signup`에서 이름, 이메일, 비밀번호를 서버에서 검증하고 중복 이메일을 차단한다.
  - 기존 `/login`에서 환경 변수 관리자와 승인된 DB 연구원 로그인을 모두 지원한다.
  - 세션에 사용자 ID와 역할을 포함하되 클라이언트 입력으로 권한을 결정하지 않는다.
  - `/admin/users`에서 가입 대기·승인·비활성 상태를 최소 기능으로 관리한다.
  - 회원이 직접 탈퇴할 수 있게 하고, 탈퇴 시 세션 종료와 계정 개인정보 삭제를 함께 처리한다.
  - 회원가입 화면에 `docs/privacy-policy.md`의 수집 항목·보유기간·동의 거부 안내와 필수 동의를 반영한다.
  - 가입·로그인 오류는 계정 존재 여부나 승인 상태를 불필요하게 노출하지 않는 문구로 처리한다.
  - 회원가입, 승인 전 로그인 거부, 승인 후 로그인, 비활성화 후 로그인 거부를 검증하는 최소 테스트를 남긴다.
- 완료 조건:
  - 새 계정의 기본 역할과 상태가 서버 및 DB에서 강제된다.
  - 비밀번호 원문이 DB와 로그에 남지 않는다.
  - 미승인·비활성 계정은 세션을 만들 수 없다.
  - 연구원 세션으로 관리자 화면과 관리자 Server Action에 접근할 수 없다.
  - 기존 관리자 로그인과 로그아웃이 계속 작동한다.
  - migration, 최소 인증 테스트, `npm run lint`, `npm run build`가 통과한다.
- Production 적용 제한:
  - 운영 DB migration과 Production 배포는 별도 승인을 받은 뒤 수행한다.
  - 이메일 인증·비밀번호 재설정 메일은 발송 서비스가 결정되기 전까지 구현하지 않는다. 이 제한을 회원가입 화면과 운영 문서에 명시한다.

### READY-08. 회원가입 오류 상태와 신청 완료 안내 개선

- 상태: `DONE`
- 우선순위: 높음
- 목적: 회원가입 실패와 성공 시 사용자가 현재 상태와 다음 행동을 분명히 알 수 있게 한다.
- 작업:
  - 비밀번호와 비밀번호 확인이 일치하지 않을 때 이름·이메일·개인정보 동의 상태를 유지한다.
  - 오류가 난 경우 비밀번호 관련 필드만 비우고, 해당 오류 메시지와 입력 위치를 접근성 있게 연결한다.
  - 비밀번호 원문은 서버 액션 반환값이나 클라이언트 상태에 보존하지 않는다.
  - 가입 신청 성공 시 폼 대신 `관리자 승인 대기 중`임을 명확히 표시한다.
  - 성공 안내에 승인 후 로그인할 수 있다는 설명과 랜딩 페이지로 돌아가는 동선을 제공한다.
  - 별도 신청 완료 페이지는 만들지 않는다. 신청 조회나 추가 온보딩 단계가 생기면 다시 검토한다.
- 완료 조건:
  - 비밀번호 불일치 후 이름·이메일·동의 값은 유지되고 두 비밀번호 필드만 비워진다.
  - 성공 안내에서 승인제 가입 절차와 다음 행동을 이해할 수 있다.
  - 중복 제출을 방지하고 관련 테스트가 통과한다.

### READY-09. 랜딩·로그인·계정 페이지 동선 정리

- 상태: `DONE`
- 우선순위: 높음
- 목적: 사용자의 인증 상태에 맞는 진입 버튼과 로그인 이후의 자연스러운 이동 경로를 제공한다.
- 작업:
  - 비로그인 사용자의 데스크톱·모바일 내비게이션에 `로그인` 버튼을 추가한다.
  - 승인된 연구자가 로그인하면 `/account`가 아니라 랜딩 페이지(`/`)로 이동하게 한다.
  - 관리자 로그인 후 `/admin`으로 이동하는 현재 동선은 유지한다.
  - 로그인한 연구자에게 `내 정보` 버튼을 제공하고 `/account`로 연결한다.
  - 로그인한 관리자에게 `관리자` 버튼을 제공하고 `/admin`으로 연결한다.
  - 기존 문의·참여 신청 CTA의 우선순위를 해치지 않는 범위에서 인증 버튼을 배치한다.
- 완료 조건:
  - 비로그인·연구자·관리자 상태별 버튼과 목적지가 데스크톱과 모바일에서 일치한다.
  - 연구자 로그인 성공 후 랜딩 페이지가 표시된다.
  - 랜딩 페이지에서 계정 또는 관리자 페이지로 이동할 수 있다.
  - 인증 상태별 내비게이션 및 리다이렉트 테스트가 통과한다.

### READY-10. 회원가입 입력 정책과 완료 문구 조정

- 상태: `DONE`
- 우선순위: 높음
- 목적: 외부 발송 서비스 없이 적용할 수 있는 OWASP 인증 입력 정책과 가입 완료 버튼 문구를 회원가입 화면에 반영한다.
- 작업:
  - 이메일은 특정 포털 도메인 목록으로 제한하지 않고 명백히 잘못된 형식과 과도한 길이만 서버에서 거부한다.
  - 회원가입 비밀번호를 최소 15자, 최대 128자로 서버에서 검증한다.
  - 비밀번호와 비밀번호 확인 입력 필드 모두 `minLength` 15, `maxLength` 128을 적용한다.
  - 영문자·숫자 조합을 요구하는 서버 정규식과 입력 필드 `pattern`을 제거하고 유니코드·공백·특수문자를 허용한다.
  - 비밀번호 도움말과 오류 문구를 `15~128자` 기준으로 변경한다.
  - 로그인 서버에서도 문자열 타입과 최대 128자를 확인한 뒤 해시를 비교한다.
  - 기존 단방향 해시·salt·키 파생 방식과 DB의 `passwordHash` 저장 형식은 변경하지 않는다.
  - 새 migration을 만들지 않는다.
  - 가입 완료 화면의 `랜딩 페이지로 돌아가기`를 `홈페이지로 돌아가기`로 변경한다.
- 완료 조건:
  - 서버에서 14자와 129자 비밀번호는 거부하고 15자와 128자 비밀번호는 허용한다.
  - 영문자·숫자·특수문자 조합 여부만으로 비밀번호를 거부하지 않는다.
  - 클라이언트와 서버의 길이 제한 및 안내 문구가 일치한다.
  - 기존 해시 생성·검증 테스트와 비밀번호 경계값 테스트가 통과한다.

### READY-11. 회원 연구 글쓰기와 자료 아카이브 분리

- 상태: `DONE`
- 우선순위: 높음
- 목적: 승인된 회원이 본인 연구 글을 임시저장하거나 즉시 공개하고, 일반 게시글이 자료 해설형 아카이브에 자동 연결되지 않게 한다.
- 확정된 정책:
  - 회원가입 자체가 관리자 승인제이므로 회원 글은 별도 게시 검수 없이 즉시 공개할 수 있다.
  - 회원 글은 공지사항·홍보자료와 구분한 전통형 연구 게시판에 노출한다.
  - 관리자 글쓰기와 회원 글쓰기는 화면과 권한을 분리하되 게시글 데이터 모델은 재사용한다.
  - 회원에게 slug를 입력받지 않고 제목을 바탕으로 서버에서 고유한 주소를 생성한다.
  - 상태 선택 UI는 없애고 `임시저장`과 `공개하기` 버튼이 내부 상태를 결정한다.
  - 자료 아카이브는 해설이 명시적으로 등록된 자료만 노출하고 일반 홍보자료 fallback을 만들지 않는다.
  - 운영 단계는 글쓰기 화면, 공개 게시글, DB 모델과 기존 데이터 구조에서 제거한다.
- 완료 조건:
  - 승인된 회원만 본인 글을 작성·수정할 수 있고 다른 회원의 글을 수정할 수 없다.
  - 공개한 회원 글만 `/research` 목록과 상세에 노출된다.
  - 일반 홍보자료는 해설 등록 없이 `/materials`와 관련 자료 목록에 노출되지 않는다.
  - 운영 단계 참조가 현재 코드와 Prisma schema에 남아 있지 않다.
  - Prisma client 생성, 게시글 테스트, lint와 build가 통과한다.
- Production 적용 제한:
  - `20260730000000_member_research_posts` migration은 저장소에만 추가하고 Production DB에는 별도 승인 없이 적용하지 않는다.

### READY-12. 연구 게시판 글쓰기·수정 진입 동선 개선

- 상태: `DONE`
- 우선순위: 높음
- 목적: 승인된 회원이 계정 관리 화면을 거치지 않고 연구 게시판에서 바로 글을 작성하거나 본인 글을 수정할 수 있게 한다.
- 작업:
  - `/research` 상단에 승인된 회원에게만 `새 글 작성` 버튼을 표시한다.
  - 버튼은 기존 `/account/posts/new` 작성 화면으로 연결한다.
  - `/research/[slug]`에서 현재 로그인한 회원이 작성자인 경우에만 `수정` 버튼을 표시한다.
  - 수정 버튼은 기존 `/account/posts/[id]/edit` 화면으로 연결한다.
  - 비로그인 사용자와 다른 회원에게는 작성·수정 버튼을 표시하지 않는다.
  - 화면 표시 여부와 별개로 기존 Server Action의 회원 승인 상태와 작성자 소유권 검사를 유지한다.
  - 새 라우트, 별도 글쓰기 화면, DB migration은 추가하지 않는다.
- 완료 조건:
  - 승인 회원은 `연구 게시판 → 새 글 작성` 한 단계로 작성 화면에 접근한다.
  - 본인 글 상세에서는 수정 화면으로 바로 이동할 수 있다.
  - 다른 회원의 글에는 수정 버튼이 없고 URL 직접 접근과 Server Action 호출도 차단된다.
  - 비로그인·승인 회원·작성자·비작성자 상태별 동작이 일치한다.
  - 데스크톱과 모바일에서 버튼 위치와 접근성을 확인한다.
  - 관련 테스트, `npm run lint`, `npm run build`가 통과한다.

### READY-13. 연구 게시판 번호 및 한글 slug 접근 오류 수정

- 상태: `DONE`
- 우선순위: 높음
- 목적: 연구 게시판 번호를 다른 게시글 유형의 DB ID와 분리하고, 한글 slug 연구 글의 공개 상세 조회가 404로 실패하는 문제를 수정한다.
- 확인된 원인:
  - `/research`가 공지·홍보자료와 공유하는 `Post.id`를 게시판 번호로 그대로 표시해 첫 연구 글도 전체 게시글 순번인 13번 등으로 보인다.
  - 연구 글 수정 경로는 숫자 `Post.id`를 사용하지만 공개 상세 경로는 slug를 사용한다.
  - 공개 상세 조회는 동적 경로의 slug를 별도 정규화 없이 DB 값과 비교하므로, 배포 환경에서 URL 인코딩된 한글 경로 값과 DB의 한글 slug가 일치하지 않을 수 있다.
- 작업:
  - DB의 `Post.id`와 저장 데이터는 변경하지 않는다.
  - 연구 게시판 안에서만 공개 글 개수를 기준으로 1부터 연속된 표시 번호를 계산한다.
  - 최신 글이 위에 오는 현재 정렬을 유지하며 화면에는 `전체 공개 글 수 - 현재 목록 인덱스` 방식으로 번호를 표시한다.
  - 동적 경로에서 받은 slug를 게시글 조회 공통 경계에서 한 번만 안전하게 디코딩한 뒤 DB와 비교한다.
  - 잘못된 percent encoding은 서버 오류를 만들지 않고 기존처럼 404로 처리한다.
  - 개별 링크마다 임시 디코딩 코드를 추가하지 않고 `getPublishedPostBySlug`를 사용하는 공지·자료·연구 상세 조회에 같은 처리를 적용한다.
  - 기존 한글 slug를 영문으로 변환하거나 DB 데이터를 다시 작성하지 않는다.
- 완료 조건:
  - 연구 게시판 번호가 전체 `Post.id`와 무관하게 게시판 내부에서 1부터 연속된다.
  - 기존 영문 slug 상세 조회가 계속 정상 동작한다.
  - 기존 한글 slug 연구 글을 게시판 제목 링크와 `내 글 관리`의 `보기` 버튼에서 열 수 있다.
  - 한글 slug의 URL 인코딩 값과 잘못된 encoding 입력에 대한 회귀 테스트가 통과한다.
  - DB migration 없이 관련 테스트, `npm run lint`, `npm run build`가 통과한다.

### READY-14. 생애사 그래프 표본 프로토타입

- 상태: `DONE`
- 우선순위: 중간
- 목적: 전체 생애사 분류가 끝나기 전에 실제 표본으로 연도 축, 일 단위 사건 밀도와 핵심 상호작용을 검증한다.
- 제공된 입력:
  - `src/lib/sample life events.csv`
  - 1977년 1월 1일부터 1994년 7월 26일까지 133건
  - `content` 있음 59건, 없음 74건
  - 같은 날짜에 여러 사건이 있는 사례 포함
  - 현재 CSV의 `date`, `title`, `content` 뒤에 있는 이름 없는 빈 열 4개는 파서에서 무시한다.
- 확정된 방향:
  - 랜딩 페이지 Hero 바로 아래에 배치한다.
  - 시안 C의 구조와 시안 A의 아이보리·금색·보라색 색상 및 문헌 질감을 기본안으로 사용한다.
  - 별도 그래프·모션 라이브러리 없이 React Client Component와 SVG, CSS로 구현한다.
  - 데이터는 현재 원문 형식인 `date`, 필수 `title`, 선택 `content`를 사용하고 날짜 입력 `YYYY/M/D`와 `YYYY/MM/DD`를 모두 허용한다.
  - 원본 날짜의 일 단위 정밀도는 정렬과 점의 실제 x좌표 계산에 사용하되 전체 보기 축에는 월·일 눈금을 표시하지 않는다.
  - 데이터와 렌더링 컴포넌트를 분리해 같은 형식의 항목을 추가하는 것만으로 점이 늘어나게 한다.
  - 점의 hover와 키보드 focus에서 날짜·제목 툴팁을 표시하고, 클릭 또는 Enter에서 그래프 아래 상세 패널을 갱신한다.
  - `content`가 없으면 상세 패널의 본문 영역을 표시하지 않는다.
  - 접근성 보조용 연대순 목록과 reduced-motion 대응을 포함한다.
- 첫 프로토타입 범위:
  - 현재 표본 범위인 1977~1994년을 사용하고 모든 연도를 x축 눈금으로 표시한다.
  - 정확한 월·일은 축 라벨이 아니라 점의 비례 위치, 툴팁, 상세 패널에 반영한다.
  - 같은 날짜 또는 가까운 날짜의 점에는 최소한의 세로 충돌 회피를 적용한다.
  - 확대·축소, 월 단위 축, 인접 점 클러스터링, 기간 막대는 구현하지 않는다.
- 데이터 형식:

```ts
type LifeEvent = {
  date: string; // YYYY/M/D 또는 YYYY/MM/DD
  title: string;
  content?: string;
};
```

- 완료 조건:
  - 데스크톱과 모바일에서 1977~1994년의 모든 연도와 사건 밀도를 읽을 수 있다.
  - 포인터, 터치, 키보드로 같은 사건을 선택하고 상세 내용을 확인할 수 있다.
  - 표본 데이터를 같은 형식으로 추가하거나 제거해도 렌더링 컴포넌트 코드를 수정하지 않는다.
  - 날짜 변환과 최소 충돌 계산 테스트, `npm run lint`, `npm run build`가 통과한다.
- 후속 전환 조건:
  - 가독성 피드백에 따라 전체 데이터 추가 전에도 연도별 클러스터와 월별 상세 보기를 `READY-16`에서 우선 구현한다.
  - 1920~2012년 전체 데이터가 추가된 뒤 실제 밀도를 기준으로 연도 라벨 간격과 클러스터 단계를 다시 조정한다.
  - 시작일과 종료일이 있는 실제 기간 데이터가 준비된 뒤에만 막대 표현과 `endDate` 추가를 검토한다.
- 구현 및 검증 결과:
  - CSV를 서버에서 읽고 검증한 뒤 직렬화 가능한 사건 배열만 Client Component에 전달한다.
  - 1977~1994년의 연도 눈금 18개와 사건 점 133개를 렌더링하고, 일 단위 비례 위치와 lane 기반 충돌 회피를 적용했다.
  - 포인터 클릭, hover, 키보드 focus·Enter, `content` 유무에 따른 상세 패널, 접을 수 있는 연대순 전체 목록을 구현했다.
  - 1280px 데스크톱과 390px 모바일에서 Hero 아래 배치, 문서 가로 넘침 없음, 모바일 그래프 내부 가로 스크롤, 상세 선택 갱신을 확인했다.
  - 브라우저 오류·경고가 없고 날짜·CSV·충돌 계산 테스트 4개, `npm run lint`, `npm run build`가 통과했다.

### READY-15. 생애사 그래프 날짜 줄바꿈과 시각 밀도 개선

- 상태: `DONE`
- 우선순위: 중간
- 목적: 상세 패널의 날짜를 한 줄로 유지하고, 사건이 많은 구간의 불필요한 세로선을 줄여 점과 연도 축을 더 쉽게 읽게 한다.
- 확인된 원인:
  - 데스크톱 상세 패널의 날짜 열이 `10rem`으로 고정된 상태에서 날짜가 `sm:text-4xl`로 커져 `1978. 12. 19.`처럼 폭이 긴 값이 줄바꿈된다.
  - 133개 사건마다 점에서 x축까지 세로 연결선을 그려 같은 시기의 선이 겹치고, 점보다 선이 먼저 보이는 시각적 소음이 생긴다.
- 작업:
  - 데스크톱 상세 패널의 날짜 열 너비를 날짜 한 줄에 필요한 만큼만 넓히고 날짜에 줄바꿈 방지를 적용한다.
  - 날짜 글자 크기와 현재 제목·본문 위계는 우선 유지하며, 전체 패널의 바깥 여백은 줄이지 않는다.
  - 각 사건 점에서 x축까지 이어지는 세로 연결선만 제거한다.
  - 가로 x축, 연도 눈금선·라벨, 사건 점의 일 단위 x좌표와 lane 배치는 유지한다.
  - hover·focus 툴팁, 클릭·Enter 선택, 상세 패널, 모바일 내부 가로 스크롤과 연대순 목록 동작은 변경하지 않는다.
  - 확대·클러스터링·필터·기간 막대나 새로운 보기 옵션은 추가하지 않는다.
- 판단 기준:
  - 세로 연결선을 제거하면 현재 화면에서 가장 반복 빈도가 높은 장식 요소가 사라져 가독성이 우선 개선될 것으로 본다.
  - 점의 정확한 날짜는 툴팁과 상세 패널에서 확인할 수 있으므로 연결선 없이도 정보 손실이 크지 않다.
  - 선을 제거한 뒤에도 점 자체의 밀도가 읽기를 방해한다는 피드백이 확인되어 `READY-16`에서 클러스터링을 우선 구현한다.
- 완료 조건:
  - 표본 데이터의 모든 날짜가 데스크톱 상세 패널에서 한 줄로 표시된다.
  - 사건별 세로 연결선은 보이지 않고 가로 축과 연도 눈금은 유지된다.
  - 데스크톱과 모바일에서 점 선택·툴팁·상세 표시가 기존과 동일하게 동작한다.
  - 페이지 가로 넘침 없이 모바일 그래프 내부 가로 스크롤이 유지된다.
  - `npm run lint`, `npm run build`가 통과한다.
- 구현 및 검증 결과:
  - 데스크톱 상세 패널의 날짜 열을 `10rem`에서 `13rem`으로 넓히고 날짜에 줄바꿈 방지를 적용했다.
  - 사건 133개의 점에서 x축까지 이어지던 세로 연결선을 제거하고 가로 축 1개와 연도 눈금 18개는 유지했다.
  - 1280px에서 `1978. 12. 19.`가 한 줄로 표시되고 문서 가로 넘침이 없으며 사건 선택이 갱신되는 것을 확인했다.
  - 390px에서 날짜 한 줄, 문서 가로 넘침 없음, 그래프 내부 가로 스크롤, 사건 133개와 선택 상태 유지를 확인했다.
  - 브라우저 오류·경고가 없고 타임라인 테스트 4개, `npm run lint`, `npm run build`가 통과했다.

### READY-16. 생애사 그래프 연도 클러스터와 월별 상세 보기

- 상태: `DONE`
- 우선순위: 높음
- 목적: 전체 연도 화면의 점 밀도를 낮추고, 필요한 연도를 확대했을 때만 월 단위 축과 모든 사건을 보여준다.
- 확정된 표시 단계:
  - 기본 `전체 연도 보기`에서는 같은 연도의 사건을 한 개의 클러스터 점으로 묶고 점 안에 사건 수를 표시한다.
  - 클러스터의 위치는 해당 연도의 사건 날짜 평균이 아니라 그 연도 구간의 중앙에 고정해 연도와 개수의 관계를 명확하게 한다.
  - 사건이 한 건인 연도도 같은 형태의 점과 숫자 `1`로 표시해 동작을 일관되게 한다.
  - 클러스터를 클릭하거나 Enter로 선택하면 해당 연도의 `월별 상세 보기`로 확대한다.
  - 월별 상세 보기에서는 선택한 연도의 1월부터 12월까지를 x축에 표시하고, 원본 일자를 비례 위치에 사용해 그 해의 모든 사건 점을 개별 표시한다.
  - 축소하면 다시 전체 연도 보기로 돌아가며, 방금 보던 연도를 기준 위치로 유지한다.
- 조작 및 상태:
  - 그래프 상단에 현재 단계와 선택 연도를 알 수 있는 간단한 확대·축소 컨트롤을 제공한다.
  - 연도 클러스터의 hover·focus에는 `연도 · 사건 N건`을 표시하고, 접근성 이름에도 연도·개수·확대 동작을 포함한다.
  - 월별 상세의 개별 점은 현재 날짜·제목 툴팁과 클릭·Enter 상세 패널 동작을 그대로 사용한다.
  - 확대 진입 시 기존 선택 사건이 해당 연도에 있으면 유지하고, 없으면 그 연도의 첫 사건을 선택한다.
  - 키보드, 포인터, 터치에서 같은 연도를 확대·축소하고 같은 사건을 선택할 수 있게 한다.
- 데이터 및 계산 구조:
  - 기존 `LifeEvent`의 `date`, `title`, `content?` 형식을 변경하지 않는다.
  - 날짜 파싱 결과를 재사용해 연도별 그룹, 사건 수, 사용 가능한 연도 목록을 데이터 계층에서 계산한다.
  - 전체 연도 보기의 클러스터 개수 합은 항상 원본 사건 수와 일치하게 한다.
  - 월별 상세는 선택 연도의 사건만 기존 lane 충돌 회피에 전달해 화면 높이와 점 밀도를 줄인다.
  - 전체 범위가 길어지면 연도 축 라벨을 1·2·5·10년 단위 중 화면에 맞는 간격으로 자동 선택하되, 데이터가 있는 연도 클러스터는 생략하지 않는다.
  - 같은 형식의 과거·미래 사건을 CSV에 추가하면 새 연도 클러스터와 연도 선택 범위가 컴포넌트 수정 없이 자동 반영되게 한다.
- 구현 범위:
  - React 상태와 현재 SVG·CSS 구조를 재사용하고 새 그래프·줌 라이브러리는 추가하지 않는다.
  - 두 단계의 이산 줌만 구현하며 마우스 휠 확대, 핀치 줌, 드래그 팬, 중간 배율은 추가하지 않는다.
  - 기간 막대, 카테고리 필터, 데이터 추가와 CSV 형식 변경은 이번 작업에 포함하지 않는다.
- 완료 조건:
  - 첫 화면에서 1977~1994년 사건 133건이 연도별 최대 한 점으로 묶이고 각 점에 정확한 개수가 표시된다.
  - 모든 클러스터 개수의 합이 133이며 사건이 누락되거나 중복되지 않는다.
  - 임의 연도를 확대하면 1~12월 눈금과 해당 연도의 모든 개별 점이 일 단위 위치에 표시된다.
  - 확대·축소 후 선택 연도와 상세 사건 상태가 예측 가능하게 유지된다.
  - 데스크톱과 모바일에서 클러스터 수치, 월 라벨, 툴팁, 상세 패널과 그래프 내부 가로 스크롤을 읽고 조작할 수 있다.
  - 연도 그룹·축 라벨 간격·선택 연도 배치 테스트, `npm run lint`, `npm run build`가 통과한다.
- 구현 및 검증 결과:
  - 전체 연도 보기에서 1977~1994년 133건을 연도별 클러스터 18개로 묶고 각 점에 사건 수를 표시했다.
  - 클러스터 합계 133건과 첫 연도 12건·마지막 연도 11건을 확인했으며, 전체 범위가 길어질 때 1·2·5·10년 간격으로 축 라벨을 줄이는 계산을 추가했다.
  - 클러스터 클릭·Enter 또는 보기 범위 선택으로 연도를 확대하고, 선택 연도의 1~12월 눈금과 모든 사건 점만 표시하도록 했다.
  - 1978년 클러스터 9건과 월별 상세 점 9개가 일치하고, 클릭·Enter 사건 선택과 상세 패널 갱신 및 전체 연도 축소 후 최근 연도 강조를 확인했다.
  - 1280px와 390px에서 문서 가로 넘침 없음, 그래프 내부 가로 스크롤, 44px 보기 범위 컨트롤, 툴팁과 상세 패널을 확인했다.
  - 브라우저 오류·경고가 없고 타임라인 테스트 6개, `npm run lint`, `npm run build`가 통과했다.

### READY-17. 생애사 그래프 확대 탐색과 복귀 동선 개선

- 상태: `READY`
- 우선순위: 높음
- 목적: 긴 연도 드롭다운 없이 연도 클러스터에서 바로 확대하고, 월별 상세에서 한 번의 명확한 조작으로 전체 연도 보기로 돌아가게 한다.
- 확인된 문제:
  - 현재 보기 범위 드롭다운은 1977~1994년 표본에서는 동작하지만 전체 데이터가 1920~2012년으로 늘면 93개 연도 항목을 탐색해야 한다.
  - 월별 상세에서 전체 연도 보기로 돌아가려면 드롭다운을 발견하고, 목록 맨 위로 이동한 뒤 `전체 연도 보기`를 선택해야 해 복귀 동선이 숨겨져 있다.
  - 현재 전체 연도 축은 고정 폭이라 93개 연도 클러스터가 추가되면 44px 이상의 조작 영역이 서로 겹칠 수 있다.
- 확정된 탐색 방식:
  - 보기 범위 `<select>`와 전체 연도·연도별 option을 모두 제거한다.
  - 전체 연도 보기에서는 기존처럼 연도 클러스터 클릭 또는 Enter만으로 해당 연도의 월별 상세에 진입한다.
  - 월별 상세 상단의 그래프 가로 스크롤 바깥에 `← 전체 연도 보기` 버튼을 항상 표시한다.
  - 같은 상단 영역에 현재 연도와 사건 수를 표시하고, `이전 연도`·`다음 연도` 버튼으로 사건이 있는 인접 연도를 이동한다.
  - 이전·다음 버튼은 실제 `yearGroups` 순서를 사용해 데이터가 없는 연도를 건너뛰며 첫 연도와 마지막 연도에서는 해당 방향을 비활성화한다.
  - 전체 연도 보기에는 별도 복귀 버튼이나 비활성 확대 컨트롤을 표시하지 않고 짧은 사용 안내만 유지한다.
- 위치 및 초점 유지:
  - 연도 클러스터를 키보드로 확대하면 해당 연도의 첫 사건 점으로 초점을 이동한다.
  - `전체 연도 보기`로 돌아오면 방금 보던 연도 클러스터를 가로 영역 중앙에 맞추고 그 점으로 초점을 복원한다.
  - 포인터로 돌아온 경우에도 같은 연도 클러스터를 강조하고 화면 안에 보이게 하되 별도 스크롤 애니메이션은 추가하지 않는다.
  - 현재 단계와 연도 변경은 기존 안내 영역의 `aria-live`로 전달한다.
- 전체 데이터 확장 대응:
  - 전체 연도 그래프 너비를 `70rem` 이상, 연도당 최소 `3.5rem` 기준으로 계산해 1920~2012년에서도 클러스터 조작 영역이 겹치지 않게 한다.
  - 연도 간 실제 공백을 보존하기 위해 사건이 있는 연도 수가 아니라 첫 연도부터 마지막 연도까지의 전체 범위를 너비 계산에 사용한다.
  - 기존 1·2·5·10년 축 라벨 간격 계산과 모바일 그래프 내부 가로 스크롤은 유지한다.
  - 데이터 추가 시 이전·다음 대상, 축 너비와 복귀 위치가 컴포넌트 수정 없이 자동 갱신되게 한다.
- 구현 범위:
  - 기존 React 상태, `yearGroups`, SVG와 네이티브 button·scroll API를 재사용하고 새 UI·모션 라이브러리는 추가하지 않는다.
  - 월별 상세의 사건 점, 툴팁, 상세 패널과 연대순 전체 목록은 변경하지 않는다.
  - 연대별 3단계 클러스터, 미니맵, 검색, 연도 직접 입력은 전체 데이터 적용 후 실제 탐색 시간이 길다고 확인될 때 검토한다.
- 완료 조건:
  - 생애사 그래프에 보기 범위 드롭다운과 연도 option이 남아 있지 않다.
  - 클러스터 클릭·Enter로 월별 상세에 진입하고 항상 보이는 `전체 연도 보기` 버튼 한 번으로 복귀할 수 있다.
  - 이전·다음 연도 버튼이 인접 데이터 연도로 이동하고 범위 양 끝에서 정확히 비활성화된다.
  - 키보드 확대 후 첫 사건, 축소 후 원래 연도 클러스터로 초점이 예측 가능하게 이동한다.
  - 1920~2012년 가상 범위에서도 클러스터 조작 영역이 겹치지 않는 축 너비가 계산된다.
  - 1280px와 390px에서 복귀 버튼·현재 연도·이전·다음 버튼과 그래프 내부 가로 스크롤을 읽고 조작할 수 있다.
  - 관련 타임라인 테스트, `npm run lint`, `npm run build`가 통과한다.

## 의사결정 또는 승인이 필요한 일

### WAITING-01. 공식 콘텐츠 반영

- 상태: `WAITING`
- 필요한 입력:
  - 생애·말씀·업적 공식 소개 원문
  - 각 주제의 핵심 질문
  - 대표 입문 자료와 공식 본문 또는 출처
  - 자료별 해설 요약과 읽기 포인트의 승인된 문안
- 승인 후 작업:
  - `src/lib/topics.ts`, 게시글 데이터, `src/lib/material-guides.ts`에 승인된 콘텐츠를 반영한다.

### WAITING-02. 개인정보 처리방침 공개 정보 확정

- 상태: `WAITING`
- 확정된 기준:
  - `docs/privacy-policy.md` 초안
  - 승인 회원정보는 탈퇴 시까지, 미승인 가입정보는 30일 보존
  - 문의 개인정보는 처리 완료 후 1년 보존
  - 접속·인증 보안기록은 3개월 보존
  - 탈퇴 시 즉시 이용 중지 후 계정 개인정보 삭제
- 필요한 입력 및 확인:
  - 개인정보 보호 담당부서, 담당자 또는 책임자
  - 개인정보 담당 이메일과 전화번호
  - Vercel·Supabase의 처리 지역, 로그·백업 보유기간과 국외이전 고지
  - 처리방침 공고일과 시행일
- 승인 후 작업:
  - 처리방침 페이지와 회원가입·문의 동의를 구현하고 footer에서 연결한다.

### WAITING-03. Vercel·Supabase 운영 설정 확정

- 상태: `WAITING`
- 확정된 사항:
  - 배포 환경은 Vercel
  - 운영 PostgreSQL은 Supabase
  - `main`은 Production, 다른 브랜치는 Preview 배포로 사용 중
- 필요한 확인 및 결정:
  - 공식 도메인
  - Vercel Production Branch가 실제로 `main`인지 여부
  - Preview와 Production DB 분리 여부
  - Supabase 요금제와 자동 백업 보존 범위
- 필요한 승인:
  - Production 환경 변수 변경
  - 운영 DB Migration 적용
  - DNS 변경과 Production 배포
- 승인 후 작업:
  - `docs/deployment-checklist.md`를 기준으로 Vercel·Supabase 설정과 Production smoke test를 완료한다.

### WAITING-04. 백업·복구·모니터링 운영 정책

- 상태: `WAITING`
- 필요한 결정:
  - 백업 주기와 보존 기간
  - 장애 알림 수신자와 채널
  - 장애 대응 담당자
  - 배포 실패 시 rollback 기준
- 필요한 승인:
  - 외부 모니터링 또는 알림 서비스 연결
  - 운영 DB 복원 시험

### WAITING-05. 소셜 로그인

- 상태: `WAITING`
- 필요한 결정:
  - Google, Kakao, Naver 등 로그인 제공자
  - 기존 이메일 계정과 소셜 계정을 연결하는 기준
  - 제공자에서 이메일을 제공하지 않거나 이메일이 중복될 때의 처리
- 필요한 승인:
  - OAuth 앱 생성과 비밀값 등록
  - OAuth 계정 연결을 위한 Prisma 모델 변경
- 승인 전 금지:
  - 소셜 로그인 provider 추가
  - OAuth 앱 또는 비밀값 생성

### WAITING-06. 커뮤니티 기능

- 상태: `WAITING`
- 필요한 결정:
  - 실제 운영 필요성
  - 이용 대상과 접근 권한
  - 게시·댓글·신고·관리 범위
  - 운영 책임자와 이용 정책
- 시작 조건:
  - 회원·권한 기능이 운영에서 검증된 뒤 별도 요청이 있을 때만 시작한다.

### WAITING-07. 보안 종합 작업

- 상태: `WAITING`
- 필요한 입력 및 승인:
  - 별도 보안 점검 요청과 허용 범위
  - 로그인·문의 요청 제한 정책
  - 운영 로그와 개인정보 취급 기준
  - Production 환경 검사 권한
  - 일반적이거나 유출된 비밀번호 차단에 사용할 로컬 목록 또는 외부 조회 방식
  - 현재 scrypt 설정 `N=2^14, r=8, p=1`을 OWASP 권장 작업 계수로 높일 때의 Vercel 성능·메모리 측정과 기존 해시 갱신 방식
- 승인 전 허용 범위:
  - 기존 코드에서 명백한 인증 누락이나 비밀 노출을 발견하면 보고만 한다.
- 승인 전 금지:
  - 외부 보안 서비스 도입
  - Production 침투·부하 시험
  - 운영 비밀값 조회 또는 변경

### WAITING-08. Apple 스타일 확장 적용

- 상태: `WAITING`
- 우선순위: 낮음
- 필요한 결정:
  - 적용할 화면 범위
  - 원하는 시각·동작 수준
  - 현재 브랜드 톤에서 유지할 요소
- 시작 조건:
  - 공식 콘텐츠와 핵심 운영 흐름이 확정된 뒤 별도 승인을 받는다.
- 승인 전 허용 범위:
  - `READY-04`의 접근성, 즉각적인 피드백, reduced-motion 같은 기본 원칙만 적용한다.

### WAITING-09. 검색엔진 및 공유 설정

- 상태: `WAITING`
- 필요한 입력:
  - 공식 도메인
  - 검색 노출 정책
  - 공유용 대표 이미지와 승인된 설명 문구
- 승인 후 작업:
  - Next.js metadata, sitemap, robots, Open Graph 정보를 실제 운영 값으로 구현한다.

### WAITING-10. 이메일 소유권·회원가입 신청·승인 결과 알림 수단 결정

- 상태: `WAITING`
- 우선순위: 높음
- 보류 이유:
  - 실제 수신자, 발신 이메일, 외부 서비스 계정 및 비밀 키 설정이 정해지지 않았다.
  - 이메일 확인 상태와 일회용 토큰을 저장하기 위한 데이터 모델 변경과 운영 DB migration 승인이 필요하다.
  - 개인정보가 외부 메시지 서비스로 전달되지 않도록 알림 본문 범위를 먼저 확정해야 한다.
- 현재 동작:
  - 신청자는 가입 완료 화면에서 관리자 승인 후 로그인할 수 있다는 안내만 받는다.
  - 이메일 소유권을 확인하지 않으며 입력한 이메일 전체를 소문자로 변환해 저장·비교한다.
  - 승인 또는 비활성화 결과를 알려주는 이메일·메시지·상태 조회 기능은 없다.
  - 승인된 경우 로그인이 가능해질 뿐, 신청자에게 승인 사실을 별도로 알리지 않는다.
- 알림 수단 비교:

| 수단 | 초기 비용 | 진입장벽 | 장점 | 주의점 | 적합한 경우 |
| --- | --- | --- | --- | --- | --- |
| Discord Incoming Webhook | webhook용 bot 운영 비용 없음 | 낮음 | 별도 bot 사용자나 인증 흐름 없이 채널 webhook으로 전송 가능 | 담당자가 Discord 서버와 채널을 사용해야 하며 webhook URL을 비밀 값으로 관리해야 함 | 담당자가 이미 Discord를 사용할 때 가장 단순함 |
| Slack Incoming Webhook | Slack workspace 요금제에 따름 | 낮음~중간 | 기존 업무 채널에 바로 쌓이고 담당자 지정이 쉬움 | Slack app 설치 및 workspace·채널 권한이 필요하며 webhook URL을 비밀 값으로 관리해야 함 | 조직이 이미 Slack을 사용할 때 적합함 |
| 이메일(Resend 등) | Resend 무료 구간은 월 3,000건·일 100건 | 중간 | 수신자가 별도 메신저에 가입할 필요가 없고 공식 기록으로 남기 쉬움 | 발신 도메인 확인, API key 및 스팸·전송 실패 관리가 필요함 | 기존 공용 메신저가 없거나 이메일이 공식 연락 수단일 때 적합함 |
| Telegram Bot | Bot API 사용 | 중간 | 소규모 담당자에게 빠르게 알릴 수 있음 | bot token·chat ID 설정이 필요하고 수신자가 Telegram을 사용해야 함 | 담당자가 이미 Telegram을 사용할 때 적합함 |

- 비교 근거: [Discord Webhooks](https://docs.discord.com/developers/resources/webhook), [Slack Incoming Webhooks](https://api.slack.com/messaging/webhooks), [Resend 요금 안내](https://resend.com/pricing?product=marketing), [Telegram Bot API](https://core.telegram.org/bots/api/)

- 권장 결정 순서:
  1. 이메일 소유권 확인과 승인 결과 발송에 사용할 이메일 서비스 하나를 선택한다.
  2. 같은 이메일 서비스로 관리자에게 확인 완료된 새 신청을 알리면 외부 연동 하나로 세 흐름을 처리할 수 있다.
  3. Slack·Discord·Telegram은 관리자 즉시 알림이 이메일만으로 부족하다고 확인된 뒤 추가한다.
- 구현 시 최소 범위:
  - 회원가입에서는 특정 이메일 도메인 허용 목록을 사용하지 않고 명백히 잘못된 형식만 거부한다.
  - 표시·발송용 원본 이메일과 로그인 비교용 정규화 값을 구분하고, 도메인 부분만 소문자로 정규화한다.
  - 회원가입 DB 저장 후 암호학적으로 안전한 확인 토큰을 생성해 가입 이메일로 확인 링크를 전송한다.
  - 확인 토큰은 일회용·만료형으로 만들고 DB에는 토큰 원문 대신 해시를 저장하며 로그에 토큰이나 전체 확인 URL을 남기지 않는다.
  - 확인 메일을 받지 못한 사용자는 동일한 일반 응답으로 재발송을 요청할 수 있게 하고 서버에서 요청 횟수를 제한한다.
  - 이메일 확인과 관리자 승인이 모두 완료된 연구자만 로그인할 수 있게 서버에서 강제한다.
  - 이메일 확인이 끝난 뒤 관리자에게 새 신청 알림을 한 번 전송한다.
  - 관리자 알림에는 이름·이메일을 포함하지 않고 `확인된 새 가입 신청`, 신청 시각, 관리자 회원 관리 화면 링크만 포함한다.
  - 관리자가 계정을 승인하거나 비활성화하면 신청자의 가입 이메일로 결과와 로그인 또는 문의 경로를 전송한다.
  - 알림 실패가 회원가입 성공을 취소하지 않게 하고 서버 로그에 실패 원인을 남긴다.
  - API key와 발신자 정보는 환경 변수로 관리한다.
  - 알림 이력 테이블, 재시도 큐, 여러 채널 동시 발송은 만들지 않는다.
- 필요한 결정:
  1. 사용할 이메일 발송 서비스
  2. 이메일 확인 링크의 유효시간
  3. 관리자 알림 수신 이메일과 신청자에게 표시할 발신자
  4. 확인·승인·비활성화 결과 안내 문구
  5. 서비스 계정 생성 및 비밀 키 등록 담당자
- 필요한 승인:
  - 이메일 확인 상태·정규화 값·확인 토큰 저장을 위한 Prisma 모델과 migration
  - 외부 이메일 발송 서비스 연결과 Production 비밀값 등록
- 결정 후 상태 변경:
  - 위 다섯 항목이 정해지고 데이터 모델·외부 서비스 사용이 승인되면 `READY`로 전환한다.

### WAITING-11. 개인정보처리방침 동의 시점과 재동의 방식 결정

- 상태: `WAITING`
- 우선순위: 높음
- 보류 이유:
  - 현재 회원가입 화면에는 필수 개인정보 동의가 있으나 공개할 개인정보처리방침 전문은 `WAITING-02`의 운영 정보가 확정되어야 완성할 수 있다.
  - 로그인할 때마다 동의를 요구할지, 최초 로그인 또는 방침 변경 때만 요구할지에 따라 UX와 DB 구조가 달라진다.
- 권장안:
  - 개인정보 수집이 시작되는 회원가입 시점에 처리방침 전문을 링크하고 필수 동의를 받는다.
  - 로그인 화면에는 처리방침 링크를 고지하되 매 로그인마다 동의를 요구하지 않는다.
  - 수집·이용 동의를 받은 사항이 변경되었을 때 `동의 버전`과 `동의 시각`을 저장해 재동의를 받는다.
  - 이 항목은 제품 구현 기본안이며 법률 자문을 대신하지 않는다. 최종 문안과 동의 방식은 [개인정보 보호법 제15조](https://www.law.go.kr/LSW/lsLinkCommonInfo.do?lsJoLnkSeq=1029334809)를 기준으로 별도 확인한다.
- 대안:
  - 최초 승인 후 첫 로그인 전에 전용 동의 화면을 한 번 표시하고, 동의하지 않으면 계정 페이지와 회원 전용 기능 접근을 막는다.
  - 이 대안을 택하면 사용자 테이블에 동의 버전·동의 시각을 저장하는 migration이 필요하다.
- 필요한 결정:
  1. 가입 시 동의만 받을지, 최초 로그인 동의를 추가할지
  2. 처리방침 변경 시 버전 기반 재동의를 구현할지
  3. `WAITING-02`의 개인정보 보호 담당자 및 연락처, Vercel·Supabase 처리 위치·국외 이전 정보, 공고일·시행일
- 결정 후 상태 변경:
  - 동의 시점과 재동의 정책, 개인정보처리방침 필수 정보가 확정되면 `READY`로 전환한다.

## Codex 작업 결과 기록

Codex는 작업을 마칠 때 아래 표에 한 줄을 추가한다.

| 날짜 | 작업 ID | 상태 | 변경 및 검증 요약 | 남은 조건 |
| --- | --- | --- | --- | --- |
| 2026-07-13 | READY-01 | DONE | README 실행 안내, 공개·관리자 경로, 단일 관리자 인증, Vercel 조건부 배포 문구를 실제 코드와 일치시킴. `npm run lint`, `npm run build` 통과. | 첫 배포 환경·공식 콘텐츠·회원 정책은 미결정 상태 유지 |
| 2026-07-13 | READY-02 | DONE | 홈 자료 영역을 실제 공개 자료 기반의 1→2→3 읽기 목록으로 변경하고 빈 상태를 분리함. 데스크톱·모바일 화면과 `주제 → 대표 자료 → 다음 자료` 이동 확인, lint/build 및 빈 상태 렌더링 검증 통과. | 공식 연구 콘텐츠는 WAITING-01 입력 전까지 기존 안내 문구 유지 |
| 2026-07-13 | READY-03 | DONE | 주제·카테고리·자료·추천 slug를 seed와 대조하고 일반 자료 fallback을 `/topics`로 수정함. 연결 assertion, 6개 상세 경로 200 응답, lint/build 통과. | slug 변경 시 `docs/sitemap.md` 연결표와 정적 설정을 함께 확인 |
| 2026-07-16 | READY-04 | DONE | 본문 바로가기, 전역 키보드 초점, 현재 메뉴 표시, 44px 주요 터치 영역, 폼 오류·상태 연결, 홈 자동 전환 제어와 reduced-motion 대응을 보완함. 데스크톱·모바일 브라우저 점검 및 lint/build 통과. | 세부 접근성 표준 적합성 감사는 별도 범위이며, 새 화면 추가 시 같은 기준으로 재점검 |
| 2026-07-16 | READY-05 | DONE | 실제 관리자 화면과 Server Action을 기준으로 로그인, 게시글 상태·slug, 문의 상태, 보관 정책과 미지원 기능을 `docs/admin-operations.md`에 정리하고 코드·경로 대조를 완료함. | 계정·배포·DB 작업과 개인정보 보존 정책은 해당 WAITING 작업 승인 후 보완 |
| 2026-07-19 | READY-06 | DONE | lint/build 통과. 공개 주요 경로와 발견된 내부 상세 링크를 데스크톱·모바일에서 확인해 정상 렌더링, 가로 넘침 없음, 브라우저 오류 로그 없음, 관리자 경로의 로그인 리디렉션을 검증함. | 로그인 후 관리자 변경·문의 성공 제출은 연결 DB 보존을 위해 생략. Production smoke test와 공개 테스트 데이터 여부는 별도 확인 필요 |
| 2026-07-20 | READY-07 | DONE | 연구자 `User` 모델과 migration, scrypt 비밀번호 해시, 가입·승인 상태별 로그인, 역할 세션, 회원 관리, 탈퇴를 구현함. 관리자 데이터 조회와 Server Action에 `ADMIN` 역할 검사를 적용하고 관리자 로그인·로그아웃 및 비로그인 보호 경로를 로컬 확인함. Prisma schema 검증·client 생성, 인증 테스트 3개, lint/build 통과. | `20260720000000_add_researcher_users`의 Production 적용·배포는 별도 승인 필요. 이메일 인증·비밀번호 재설정과 소셜 로그인은 미구현 상태 유지 |
| 2026-07-21 | READY-08 | DONE | 회원가입 오류 필드를 Server Action 상태로 식별하고 오류 메시지·포커스를 연결함. 비밀번호 불일치 후 이름·이메일·동의는 유지하고 두 비밀번호만 비우며, 성공 시 폼을 승인 대기 안내와 랜딩 이동 링크로 대체함. 브라우저 오류 흐름, 인증 테스트 4개, lint/build 통과. | 성공 제출은 Production 테스트 계정 생성을 피하기 위해 수행하지 않음. 별도 완료 페이지와 신청 조회는 추가하지 않음 |
| 2026-07-22 | READY-09 | DONE | 세션 역할에 따라 비로그인은 `로그인`, 연구자는 `내 정보`, 관리자는 `관리자` 버튼을 데스크톱·모바일 내비게이션에 표시함. 연구자 로그인 후 홈, 관리자 로그인 후 관리자 화면으로 이동하도록 정리함. 역할별 인증 테스트 6개, 데스크톱·390px 모바일 브라우저 점검, lint/build 통과. | 없음 |
| 2026-07-26 | READY-10 | DONE | 가입 비밀번호를 15~128자로 변경하고 문자 조합 제한을 제거했으며, 로그인에서도 128자 초과 입력을 해시 비교 전에 거부함. 두 입력 필드의 제한·안내 문구와 완료 링크 문구를 맞춤. 경계값·기존 해시 포함 인증 테스트 7개, 브라우저 렌더링 점검, lint/build 통과. | 이메일 소유권 확인과 유출 비밀번호·scrypt 강도 검토는 각각 WAITING-10·WAITING-07 유지 |
| 2026-07-30 | READY-11 | DONE | 작성자 관계와 `RESEARCH` 유형, 회원 본인 글 관리, `/research` 게시판, 자동 slug, 임시저장·즉시 공개를 구현함. 운영 단계를 제거하고 명시적 해설이 없는 홍보자료의 아카이브 fallback 연결을 차단함. Prisma validate, 인증 7개·게시글 3개 테스트, lint/build 통과. | `20260730000000_member_research_posts` Production migration과 배포 환경 smoke test는 별도 승인 필요 |
| 2026-07-31 | READY-12 | DONE | 승인 회원에게 `/research`의 `새 글 작성` 버튼을, 본인 공개 글 상세에 `수정` 버튼을 표시하고 기존 작성·수정 화면으로 연결함. 기존 승인 상태·작성자 소유권 검사는 유지함. 소유권 포함 게시글 테스트 4개, 데스크톱·390px 모바일 비로그인 화면과 직접 접근 차단, lint/build 통과. | 승인 회원 상태의 배포 환경 smoke test는 READY-11 migration 적용 후 확인 |
| 2026-08-09 | READY-13 | DONE | 연구 게시판 표시 번호를 공개 글 수 기준의 연속 번호로 바꾸고, 공개 상세 조회 공통 경계에서 slug를 안전하게 한 번 디코딩함. 실제 목록의 `2, 1` 번호와 한글 인코딩·영문 slug 상세 200, 잘못된 percent 값 404를 브라우저에서 확인함. 게시글 테스트 5개, lint/build 통과. | 새 migration과 기존 slug 데이터 변경 없음 |
| 2026-08-12 | READY-14 | DONE | Hero 아래에 C 구조·A 색상/질감의 React·SVG 생애사 그래프를 추가함. CSV 133건, 연도 18개, 일 단위 위치, 충돌 lane, 툴팁, 클릭·Enter 상세 패널, 연대순 목록을 구현함. 1280px·390px 브라우저와 오류 로그를 확인하고 타임라인 테스트 4개, lint/build 통과. | 연도 클러스터·월별 상세는 READY-16에서 우선 구현. 기간 데이터가 생길 때만 막대 검토 |
| 2026-08-12 | READY-15 | DONE | 상세 날짜 열을 넓히고 줄바꿈을 방지했으며 사건별 세로 연결선을 제거함. 1280px·390px에서 날짜 한 줄, 축·눈금·점·선택 동작, 모바일 내부 스크롤과 문서 폭을 확인하고 타임라인 테스트 4개, lint/build 통과. | 점 밀도 피드백에 따라 READY-16에서 클러스터링 우선 구현 |
| 2026-08-13 | READY-16 | DONE | 133건을 연도별 클러스터 18개로 묶어 수치를 표시하고, 선택 연도의 12개월 축과 개별 사건 점으로 확대하는 두 단계 보기를 구현함. 클릭·Enter·범위 선택, 1280px·390px 내부 스크롤과 문서 폭을 확인하고 타임라인 테스트 6개, lint/build 통과. | 드롭다운 제거와 복귀·인접 연도 동선은 READY-17에서 개선. 전체 데이터 적용 후 추가 클러스터 단계 재검토 |

## 사용자 결정 기록

사용자가 결정을 내리면 아래 표에 기록하고 해당 `WAITING` 작업을 `READY`로 변경한다.

| 날짜 | 관련 작업 ID | 결정 또는 승인 | 적용 범위 |
| --- | --- | --- | --- |
| 2026-07-15 | WAITING-03 | 첫 배포 환경은 Vercel, 운영 DB는 Supabase로 결정 | 배포·DB 관련 문서와 후속 작업 |
| 2026-07-19 | WAITING-05 | 이메일·비밀번호 회원가입과 일반 로그인을 READY로 전환하고 소셜 로그인은 WAITING 유지 | READY-07과 WAITING-05 |
| 2026-07-19 | WAITING-02 | 일반적인 개인정보 보존·탈퇴·삭제 기준을 `docs/privacy-policy.md` 초안으로 확정 | READY-07과 개인정보 처리방침 공개 준비 |
| 2026-07-21 | READY-10 | 회원가입 비밀번호를 12~20자로 제한하고 기존 해시 방식과 DB 저장 형식은 유지 | 회원가입 검증·입력 안내·경계값 테스트 |
| 2026-07-22 | READY-10, WAITING-10 | 12~20자 정책을 철회하고 OWASP 기준의 이메일 소유권 확인과 비밀번호 정책을 적용 | 비밀번호 15~128자·모든 문자 허용, 이메일 확인 후 관리자 승인 및 로그인 |
| 2026-07-30 | READY-11 | 승인 회원 글은 관리자 검토 없이 즉시 공개하며, 별도 연구 게시판에 노출하고 자료 아카이브 자동 연결은 제거 | 회원 글쓰기·연구 게시판·운영 단계 제거·자료 아카이브 선별 연결 |
| 2026-07-31 | READY-12 | 연구 게시판에서 승인 회원이 바로 새 글을 작성하고 본인 글 상세에서 수정 화면으로 이동할 수 있게 하는 UX 개선을 승인 | 연구 게시판 목록·상세의 역할 및 소유권 기반 작성·수정 진입점 |
| 2026-08-12 | READY-14 | 생애사 그래프는 시안 C의 구조와 시안 A의 색상·질감으로 시작하고, 제공된 1977~1994년 표본에서는 모든 연도를 표시하며 확대·월 눈금·클러스터링은 전체 분류 후로 보류 | Hero 아래 생애사 그래프, `date`·`title`·`content?` 데이터, React·SVG 기반 점 표현 |
| 2026-08-13 | READY-16 | 데이터 추가보다 현재 점 밀도 개선을 우선하고, 축소 상태에서는 인접 사건을 묶어 개수를 표시하며 확대 상태에서는 월 축과 모든 점을 표시 | 생애사 그래프의 연도별 클러스터와 월별 상세 보기 |
| 2026-08-13 | READY-17 | 1920~2012년 확장을 고려해 연도 드롭다운을 제거하고, 월별 상세에서 전체 연도 복귀와 인접 연도 이동을 항상 보이는 버튼으로 제공 | 생애사 그래프의 확대·축소·연도 이동 동선 |
